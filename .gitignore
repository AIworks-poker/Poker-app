/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo

# env files — .env.local is generated by `vercel env pull` and holds live
# credentials. NEVER commit: this repo is PUBLIC (github.com/AIworks-poker/Poker-app).
.env*

.b.log
.npm.log
.deploy.log
.vercel

# Secrets and internal notes kept in the repo folder but out of the repo.
# The public-repo point above is why CLAUDE.md is here: the project brain names
# internal paths, the admin address and tracker IDs. If the repo is ever made
# private, revisit whether the brain should be tracked (it is the only copy).
poker-*.txt
CLAUDE.md
